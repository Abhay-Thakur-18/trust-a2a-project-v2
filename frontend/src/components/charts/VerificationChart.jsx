import VerificationTrendChart from "./VerificationTrendChart";

function VerificationChart({ verifications = [] }) {
  return <VerificationTrendChart verifications={verifications} />;
}

export default VerificationChart;
